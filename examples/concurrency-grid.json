{
  "title": "Four ways to guard a resource",
  "highlight": "Concurrency in .NET",
  "subtitle": "Pick by what you are protecting and whether the caller is async.",
  "tags": [".NET 9", "Threading", "Locks"],
  "issue": "05",
  "layout": "grid",
  "theme": "vesper",
  "insight": "lock and Lock are for short critical sections on one process. Semaphores bound concurrency; only SemaphoreSlim can be awaited.",
  "panels": [
    {
      "label": "lock statement",
      "language": "csharp",
      "underline": ["object _gate"],
      "code": "private readonly object _gate = new();\n\npublic void Increment()\n{\n    lock (_gate)\n    {\n        _count++;\n    }\n}"
    },
    {
      "label": "System.Threading.Lock",
      "language": "csharp",
      "underline": ["Lock _gate"],
      "code": "private readonly Lock _gate = new();\n\npublic void Increment()\n{\n    lock (_gate)\n    {\n        _count++;\n    }\n}"
    },
    {
      "label": "Semaphore",
      "language": "csharp",
      "underline": ["Semaphore _slots"],
      "code": "private static readonly Semaphore _slots = new(3, 3);\n\npublic void Access()\n{\n    _slots.WaitOne();\n    try { DoWork(); }\n    finally { _slots.Release(); }\n}"
    },
    {
      "label": "SemaphoreSlim",
      "language": "csharp",
      "underline": ["SemaphoreSlim _slots", "await"],
      "code": "private static readonly SemaphoreSlim _slots = new(3);\n\npublic async Task AccessAsync()\n{\n    await _slots.WaitAsync();\n    try { await DoWorkAsync(); }\n    finally { _slots.Release(); }\n}"
    }
  ]
}
