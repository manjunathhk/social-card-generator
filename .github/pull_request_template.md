<!--
Fill in the sections below and delete this comment block. Keep whichever
checklist items apply and mark the rest N/A — don't delete them, a
reviewer needs to see what was considered, not just what was done.
-->

## Summary

<!-- 1-3 bullets: what changed and why (the "why" matters more than the "what" — the diff already shows what). -->

## Test plan

<!-- Bulleted checklist of what you ran or verified, e.g. `npm run check`, `npm test`, manual steps in a browser. -->

## Checklist

- [ ] `version` in `package.json` bumped (see `CONTRIBUTING.md`, "Releasing"), **or** this PR is labeled `no-version-bump` because it ships nothing release-worthy (docs/CI/test-only). CI enforces this on every PR — see the "Version bump check" job.
- [ ] Docs (`README.md`, `docs/`, `CONTRIBUTING.md`, `AGENTS.md`) reviewed for anything this change makes stale — commands, flags, file paths, screenshots, counts/examples. N/A if this PR doesn't touch behavior docs describe.
